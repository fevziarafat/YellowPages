@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var contact = ViewBag.phone as YellowPagesDto;

    var d = contact;
}

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">YellowPages</h5>
                @{
                    if (contact != null)
                    {
                        <table class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>İsim</th>
                                    <th>Soyad</th>
                                    <th>Firma</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>@contact.name</td>
                                    <td>@contact.surName</td>
                                    <td>@contact.firm</td>
                                </tr>
                                <tr>
                                    @if ((@contact.phoneInformations as List<PhoneInformationDto>) != null)
                                    {
                                    <tr>
                                        <th>Phone</th>
                                        <th>İşlemler</th>


                                    </tr>
                                        @foreach (var item in @contact.phoneInformations as List<PhoneInformationDto>)
                                        {
                                        <tr>
                                            <td>@item.Phone</td>
                                            <td>   <a asp-controller="YellowPages" asp-action="PhoneDelete2" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Sil</a></td>
                                        </tr>



                                        }


                                    }
                                </tr>
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="alert alert-info">
                            Kayıtlı Phone bulunamadı
                        </div>
                    }
                }
            </div>
        </div>
    </div>
</div>